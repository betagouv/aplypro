.fr-select-group.fr-col-md-7
  = form_with model: @user, builder: DsfrFormBuilder do |form|
    .fr-input-group
      = form.label :selected_establishment_id, "Établissement", class: 'fr-label'
      = form.select :selected_establishment_id, @user.establishments.map { |e| [e.select_label, e.id] }, {}, { class: 'fr-select' }

    = form.submit "Continuez avec cet établissement", class: 'fr-btn'
