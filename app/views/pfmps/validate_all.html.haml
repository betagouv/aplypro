- if @pfmps.none?
  .fr-col-md-6
    %p Tout est à jour !

    = dsfr_alert(type: :success, title: "Aucune PFMP à valider") do
      Il n'y aucune PFMP à compléter.
- else
  %h3
    = @pfmps.count
    PFMPs restantes à valider.

  %hr

  = form_with url: pfmp_validate_path(@pfmp), method: :post do |form|
    = render 'pfmp_breakdown'
    .fr-btns-group.fr-btns-group--inline.fr-mt-6w
      = form.submit "Valider cette PFMP et passer à la suivante", class: 'fr-btn'
      = dsfr_button label: "Ignorer et passer à la suivante", level: :secondary
