- if payment_requests.last.failed? && !payment_requests.active.any?
  .fr-callout.fr-icon-information-line
    %h3.fr-callout__title=t("payment_requests.actions.reset.title")
    .fr-callout__text
    = form_with method: :post, url: reset_payment_request_class_schooling_pfmp_path(@classe, @schooling, @pfmp) do |form|
      = t("payment_requests.actions.reset.warning")
      = render partial: "shared/confirmed_director"
      = form.submit t("payment_requests.actions.reset.cta"), class: "fr-btn"
