= dsfr_alert(type: :warning, title: "Les dates sont indicatives") do
  %ul
    - string_end_date = schooling.max_end_date.present? ? "#{schooling.max_end_date.strftime('%d/%m/%Y')}" : "01/09/#{schooling.classe.school_year.end_year}"
    %li= "Les dates saisies doivent être comprises dans la durée de la scolarité de l'élève (entre le #{schooling.start_date.strftime('%d/%m/%Y')} et le #{string_end_date})."

    %li Les dates saisies doivent correspondre à celles qui figurent dans la convention de stage.

    %li Le nombre de jours doit correspondre à celui qui figure dans l'attestation de fin de stage.
